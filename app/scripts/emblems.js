/********************************************************
 * Emblems
 *
 * Emblems for markers.
 ********************************************************/
define(['jquery', 'settings'], function($, settingsFabric) {

        var emblemsFabric = {
            add: function(mapLocationId, emblem) {

            },
            remove: function(mapLocationId, emblem) {

            }
        }

        return emblemsFabric;
    }
);
