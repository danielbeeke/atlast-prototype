/********************************************************
 * Filters
 *
 * Filters for the map.
 ********************************************************/
define(['jquery', 'map', 'menu'], function($, mapFabric, menuFabric) {

        var filtersFabric = {
            init: function() {

            }
        }

        return filtersFabric;
    }
);